# Performance-Evaluation-of-Algorithms-in-OS
A clean, beginner-friendly web app that **automatically selects and explains the best Operating System algorithm** for your problem — no AI, just smart rule-based logic.  Built with **Python (Flask)** + plain HTML/CSS/JS. No database, no login, no heavy frameworks.
